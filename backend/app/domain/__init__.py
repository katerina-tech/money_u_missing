"""Domain types.

Pure data and pure functions: no I/O, no database, no model calls. Everything
here can be constructed in a test without a fixture, which is what makes the
matching and money rules cheap to pin down exhaustively.
"""
